apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base kustomization for Spark-Livy deployment
# This file references the base manifest only (no version defaults)
# Overlays in ../overlays/* define specific versions via patches

resources:
  - spark-livy-manifest.yaml
