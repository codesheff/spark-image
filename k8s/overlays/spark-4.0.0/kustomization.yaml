# ⚠️  AUTO-GENERATED FILE
# This file is generated by scripts/generate-overlays.sh
# DO NOT EDIT DIRECTLY - use the script to regenerate all overlays
# To add a new version or modify: edit scripts/generate-overlays.sh and run it

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Overlay for Spark 4.0.0 + Livy 0.9.0
# Note: PySpark has known compatibility issues with Python 3.10+
# Scala sessions work perfectly. For PySpark, use spark-3.5.7 overlay instead.
# Usage: kubectl apply -k k8s/overlays/spark-4.0.0/

resources:
  - ../../base

# Override image tags
images:
  - name: stedoh/spark-livy
    newTag: "4.0.0"

# Patch ConfigMap pod templates with 4.0.0 images
patchesJson6902:
  - target:
      group: ""
      version: v1
      kind: ConfigMap
      name: spark-driver-pod-template
    patch: |-
      - op: replace
        path: /data/driver-pod-template.yaml
        value: |
          apiVersion: v1
          kind: Pod
          spec:
            containers:
            - name: spark-driver
              image: stedoh/spark-livy:4.0.0
              imagePullPolicy: IfNotPresent
              resources:
                requests:
                  cpu: 1000m
                  memory: 2Gi
                limits:
                  cpu: 2000m
                  memory: 4Gi
              securityContext:
                runAsUser: 1000
                runAsGroup: 1000
              volumeMounts:
              - name: spark-events
                mountPath: /tmp/spark-events
              volumes:
              - name: spark-events
                emptyDir: {}
  - target:
      group: ""
      version: v1
      kind: ConfigMap
      name: spark-executor-pod-template
    patch: |-
      - op: replace
        path: /data/executor-pod-template.yaml
        value: |
          apiVersion: v1
          kind: Pod
          spec:
            containers:
            - name: spark-executor
              image: stedoh/spark-livy:4.0.0
              imagePullPolicy: IfNotPresent
              resources:
                requests:
                  cpu: 1000m
                  memory: 2Gi
                limits:
                  cpu: 2000m
                  memory: 4Gi
              securityContext:
                runAsUser: 1000
                runAsGroup: 1000
              volumeMounts:
              - name: spark-events
                mountPath: /tmp/spark-events
              volumes:
              - name: spark-events
                emptyDir: {}

# Add version labels (to pods only, not to selector - selector is immutable)
labels:
  - includeSelectors: false
    pairs:
      spark-version: "4.0.0"
      livy-version: "0.9.0"
