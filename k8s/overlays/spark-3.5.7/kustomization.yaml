# ⚠️  AUTO-GENERATED FILE
# This file is generated by scripts/generate-overlays.sh
# DO NOT EDIT DIRECTLY - use the script to regenerate all overlays
# To add a new version or modify: edit scripts/generate-overlays.sh and run it

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Overlay for Spark 3.5.7 + Livy 0.8.0
# Recommended for production with full PySpark support
# Usage: kubectl apply -k k8s/overlays/spark-3.5.7/

resources:
  - ../../base

# Override image tags
images:
  - name: stedoh/spark-livy
    newTag: "3.5.7"

# Patch ConfigMap pod templates with 3.5.7 images
patchesJson6902:
  - target:
      group: ""
      version: v1
      kind: ConfigMap
      name: spark-driver-pod-template
    patch: |-
      - op: replace
        path: /data/driver-pod-template.yaml
        value: |
          apiVersion: v1
          kind: Pod
          spec:
            containers:
            - name: spark-driver
              image: stedoh/spark-livy:3.5.7
              imagePullPolicy: IfNotPresent
              resources:
                requests:
                  cpu: 1000m
                  memory: 2Gi
                limits:
                  cpu: 2000m
                  memory: 4Gi
              securityContext:
                runAsUser: 1000
                runAsGroup: 1000
              volumeMounts:
              - name: spark-events
                mountPath: /tmp/spark-events
              volumes:
              - name: spark-events
                emptyDir: {}
  - target:
      group: ""
      version: v1
      kind: ConfigMap
      name: spark-executor-pod-template
    patch: |-
      - op: replace
        path: /data/executor-pod-template.yaml
        value: |
          apiVersion: v1
          kind: Pod
          spec:
            containers:
            - name: spark-executor
              image: stedoh/spark-livy:3.5.7
              imagePullPolicy: IfNotPresent
              resources:
                requests:
                  cpu: 1000m
                  memory: 2Gi
                limits:
                  cpu: 2000m
                  memory: 4Gi
              securityContext:
                runAsUser: 1000
                runAsGroup: 1000
              volumeMounts:
              - name: spark-events
                mountPath: /tmp/spark-events
              volumes:
              - name: spark-events
                emptyDir: {}

# Add version labels (to pods only, not to selector - selector is immutable)
labels:
  - includeSelectors: false
    pairs:
      spark-version: "3.5.7"
      livy-version: "0.8.0"
